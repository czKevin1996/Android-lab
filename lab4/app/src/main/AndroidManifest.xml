<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kebo.lab4">

    <application
        android:name="com.example.kebo.lab4.ShoppingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="lab4"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.kebo.lab4.MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".Receiver">
            <intent-filter>
                <action android:name="com.example.kebo.lab4.staticaction"></action>
            </intent-filter>
        </receiver>
        <activity android:name="com.example.kebo.lab4.DetailActivity"></activity>
    </application>

</manifest>